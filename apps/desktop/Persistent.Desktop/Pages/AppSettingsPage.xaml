<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Persistent.Desktop.Pages.AppSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer Padding="32,16,32,32">
        <StackPanel Spacing="8" MaxWidth="760" HorizontalAlignment="Left">
            <TextBlock Text="Settings" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,8" />

            <!-- Theme. The last setting still here, and it belongs here: it themes
                 this window, which is chrome the page cannot see. Everything about
                 what the app DOES for the user moved onto the page's own Settings
                 screen, so there is one Settings screen rather than two. -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                    CornerRadius="8" Padding="16,12">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="App theme" FontWeight="SemiBold" />
                        <TextBlock Opacity="0.7" TextWrapping="Wrap"
                                   Text="Appearance of this window. Persistent itself has its own theme, set inside the app." />
                    </StackPanel>
                    <ComboBox Grid.Column="1" x:Name="ThemeCombo" VerticalAlignment="Center" MinWidth="160"
                              SelectionChanged="ThemeCombo_SelectionChanged">
                        <ComboBoxItem Content="System default" />
                        <ComboBoxItem Content="Light" />
                        <ComboBoxItem Content="Dark" />
                    </ComboBox>
                </Grid>
            </Border>

            <!-- Where the rest went. Anyone who used to change notifications here
                 will come back here to look for them, so say so and take them
                 there rather than leaving them to search. -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                    CornerRadius="8" Padding="16,12">
                <StackPanel Spacing="10">
                    <StackPanel>
                        <TextBlock Text="Notifications, startup and the flyout" FontWeight="SemiBold" />
                        <TextBlock Opacity="0.7" TextWrapping="Wrap"
                                   Text="These are in Persistent itself now, on its Settings screen, alongside its theme, sounds and time format." />
                    </StackPanel>
                    <Button x:Name="OpenAppSettingsButton" Click="OpenAppSettingsButton_Click"
                            Content="Open Persistent settings" HorizontalAlignment="Left" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
